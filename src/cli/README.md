# Statute CLI (Local Execution)

This folder contains the **local** Governance-as-Code CLI implementation.

Run locally from this repo:
- `npm run statute -- init`
- `npm run statute -- scan`
- `npm run statute -- fix`

Local state is stored in `.statute/` (ignored by git).

